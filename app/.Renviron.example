# =============================================================================
# AXP MVP Survey - Shiny App Environment Variables
# =============================================================================
# COPY this file to app/.Renviron on the server and set real values.
# NEVER commit actual secrets to git.
#
# Location on server: /srv/shiny-server/axp-mvp-survey/app/.Renviron
# Permissions: chmod 600, chown shiny:shiny
# =============================================================================

# -----------------------------------------------------------------------------
# Database (MariaDB - self-hosted on vServer)
# -----------------------------------------------------------------------------
# For localhost connections, DB_SSL=false is correct (TLS not needed).
# TLS is only required for remote database connections.

DB_DIALECT=mariadb
DB_HOST=127.0.0.1
DB_PORT=3307
DB_NAME=axp_mvp
DB_USER=axp_app
DB_PASSWORD=your-secret-password-here
DB_SSL=false

# -----------------------------------------------------------------------------
# Google Sheets (questionnaire source)
# -----------------------------------------------------------------------------
# Service account authentication (recommended for server deployments)

GOOGLE_SHEET_ID=your-google-sheet-id
GOOGLE_SHEET_SHEETNAME=Sheet1
GOOGLE_SHEET_AUTH_JSON=/srv/shiny-server/axp-mvp-survey/secret/your-service-account.json
GOOGLE_SHEET_AUTH_EMAIL=your-service-account@project.iam.gserviceaccount.com
GOOGLE_SHEET_USE_OAUTH=false

# -----------------------------------------------------------------------------
# OSF Export (public data uploads)
# -----------------------------------------------------------------------------
# Get token from: https://osf.io/settings/tokens

OSF_PROJECT_ID=your-osf-project-id
OSF_TOKEN=your-osf-personal-access-token

# Salt for anonymizing session IDs in public exports (use a random string)
EXPORT_ANON_SALT=change-this-to-a-random-string

# -----------------------------------------------------------------------------
# App Settings
# -----------------------------------------------------------------------------

DEV_MODE=false
APP_BASE_URL=https://your-domain.com

# P6M animated background (optional)
P6M_ENABLED=false
P6M_ANIMATED=false
