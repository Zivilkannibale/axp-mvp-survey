# Google Sheets configuration
GOOGLE_SHEET_CSV_URL="<paste google sheet published-as-csv URL here>"
GOOGLE_SHEET_ID="<gsheet id>"
GOOGLE_SHEET_SHEETNAME="<worksheet name>"
GOOGLE_SHEET_AUTH_JSON="<path to service account json>"
GOOGLE_SHEET_AUTH_EMAIL="<service account email>"
GOOGLE_SHEET_USE_OAUTH="false"
GARGLE_OAUTH_CACHE="<path to gargle oauth cache>"

# Database configuration (MariaDB - primary)
DB_DIALECT="mariadb"
DB_HOST="database-5019530911.webspace-host.com"
DB_PORT="3306"
DB_NAME="dbs15265782"
DB_USER="dbu4550099"
DB_PASSWORD="<password - NEVER commit this>"
DB_TLS="1"
DB_TLS_VERIFY="1"
DB_TLS_CA_PATH=""

# Legacy PostgreSQL configuration (backward compatibility)
# STRATO_PG_HOST="<host>"
# STRATO_PG_DB="<db>"
# STRATO_PG_USER="<user>"
# STRATO_PG_PASSWORD="<password>"
# STRATO_PG_PORT="5432"

# OSF export configuration
OSF_PROJECT_ID="<osf project id>"
OSF_TOKEN="<token - NEVER commit this>"
OSF_FOLDER="data"

# Export anonymization salt (keep secret and consistent across exports)
EXPORT_ANON_SALT="<random string - NEVER commit this>"

# App configuration
APP_BASE_URL="<https url>"
P6M_ENABLED="true"
P6M_ANIMATED="true"
DEV_MODE="false"
